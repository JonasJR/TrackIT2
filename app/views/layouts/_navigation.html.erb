<nav class="navbar">
<div class="container-fluid">
<!-- Brand and toggle get grouped for better mobile display -->
<div class="navbar-header">
  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
    <span class="sr-only">Toggle navigation</span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
  </button>
  <a class="navbar-brand" href="/">Track<span class="spans">I</span>t</a>
</div>
<!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav">
    <li class="<%= is_active?('/') %>"><%= link_to "Home", '/' %></li>
    <% if signed_in? %>
      <li class="<%= is_active?('/proposals/new') %>"><%= link_to "Submit", '/proposals/new' %></li>
    <% else %>
      <li><a href="#" data-toggle="modal" data-target="#loginModal">Submit</a></li>
    <% end %>
    <li class="<%= is_active?('/proposals') %>"><%= link_to "Browse", '/proposals' %></li>
    <% if current_user.try(:teacher?) %>
      <li class="<%= is_active?('/teachers/approve') %>"><%= link_to "Approve", '/teachers/approve' %></li>
    <% else %>
    <% end %>
      </ul>
    </li>
  </ul>
  <ul class="nav navbar-nav navbar-right">
    <% if signed_in? %>
      <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></a></li>
    <% else %>
    <li><a href="#" data-toggle="modal" data-target="#loginModal">Sign in / Sign up</a></li>
    <% end %>
  </ul>

</div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>
